{
  "name": "fishsmart-pro-monorepo",
  "version": "2.37.0",
  "private": true,
  "workspaces": [
    "app"
  ],
  "scripts": {
    "dev": "npm run dev -w app",
    "start": "npm run start -w app",
    "build:css": "npm run build:css -w app",
    "test": "npm run test -w app",
    "sync-versions": "node tools/sync-versions.js",
    "sync-versions:write": "node tools/sync-versions.js --write",
    "bump": "node tools/sync-versions.js --bump",
    "bump:patch": "node tools/sync-versions.js --bump-patch",
    "version:check": "node tools/sync-versions.js --check",
    "prepare": "node tools/setup-hooks.js",
    "predev": "npm run build:css"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
